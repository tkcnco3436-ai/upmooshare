# 공유일지

**언제 · 누구에게 · 무엇을 공유했는지**의 이력. 깃에 올라가는 정본이다.

날짜별 작업 폴더(`YYYYMMDD_업무공유/`)는 깃에 올리지 않는다. 여기에 한 줄만 남긴다.

---

## 기록 (최신이 위)

| 일자 | 대상 | 공유 내용 | 채널 | 공개 리포트 | 커밋 |
| --- | --- | --- | --- | --- | --- |
| 2026-07-29 | 팀 | V1.0.0 로드맵 전체 완료 표시 | — | `roadmap-0720` | `e1720f4` |
| 2026-07-28 | 개발·팀 | 0728 업무공유 (로그인·회원가입 / 알림 / 크레딧 화면) + 전달 폴더 | — | `0728` | `6dc66f5` |
| 2026-07-23 | 개발·팀 | 0723 업무공유 | — | `0723` | — |
| 2026-07-22 | 개발·팀 | 0722 업무공유 | — | `0722` | — |
| 2026-07-20 | 개발·팀 | 0720 업무공유 (마이페이지/로그인 화면) · 구글 애널리틱스 · V1.0.0·V1.5.0 로드맵 | — | `0720` `ga-0720` `roadmap-0720` | — |

> `채널` 은 실제 발송 경로(디스코드·메일·구두). 확인되지 않은 과거 기록은 `—` 로 둔다.
> `공개 리포트` 는 `index.html` 의 `data-report-id` 값.

---

## 폴더 구조

```
보고서및공유/
├─ .gitignore              # 날짜별 폴더 제외
├─ 공유일지.md              # 이 파일 — 깃 정본
├─ 말투설정.md              # 이 폴더 전용 페르소나·말투
├─ CLAUDE.md               # 이 폴더 작업 규칙 (말투 적용 지시 포함)
├─ index.html              # 공개 리포트 사이트 (GitHub Pages) — 깃
├─ YYYYMMDD업무공유.md      # 깃용 날짜별 공유내용 (리포트 원본) — 깃
├─ 보고서디자인.md · 상인월드-소개서.md                        — 깃
├─ image/ · 0728_개발전달_공유/                               — 깃
│
└─ YYYYMMDD_업무공유/       # 🚫 깃 제외 (로컬 작업용)
   ├─ 업무공유내용.md
   └─ 디스코드용채팅공유.md
```

**깃에 올라가는 것**: 공유일지 · 말투설정 · CLAUDE.md · `index.html` · 깃용 날짜별 `YYYYMMDD업무공유.md` · 이미지 · 전달 폴더
**깃에 올라가지 않는 것**: `YYYYMMDD_업무공유/` 날짜별 작업 폴더 전체

---

## 발행 절차

1. `YYYYMMDD_업무공유/` 폴더를 만든다 (파일 2개만: `업무공유내용.md` · `디스코드용채팅공유.md`)
2. `업무공유내용.md` 에 내용을 정리한다 — **작업 원본, 깃에 안 올라감**
3. 확정되면 **깃용으로 별도 구성**한다
   - 루트에 `YYYYMMDD업무공유.md` 생성 (리포트 원본)
   - `index.html` 에 `<script type="text/markdown" data-report-id="MMDD" …>` 블록 추가
4. `디스코드용채팅공유.md` 문구를 다듬어 디스코드에 발송
5. **이 파일(`공유일지.md`)에 한 줄 추가**
6. 커밋·푸시 (`origin main`) → GitHub Pages 반영 확인
7. 워크스페이스 규칙에 따라 `../시스템에러/` 에 로그 기록

## 주의

- 로컬 폴더명은 `보고서및공유` 지만 **원격 저장소는 `upmooshare`** 다. 공개 URL은 `tkcnco3436-ai.github.io/upmooshare/` 로 유지된다
- 과거 일자 리포트(`data-report-id="0720"` 등)는 **그날의 기록**이다. 나중에 상태가 바뀌어도 고치지 않는다. 갱신은 로드맵 같은 생존 문서에서만 한다
- 공개 페이지이므로 키·계정·내부 URL을 넣지 않는다

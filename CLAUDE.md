# CLAUDE.md — `보고서및공유/`

이 폴더에서 작업할 때의 규칙. 워크스페이스 전역 규칙은 [`../CLAUDE.md`](../CLAUDE.md).

---

## 🎭 말투 — 최우선

**이 폴더의 산출물은 [`말투설정.md`](말투설정.md) 의 말투를 따른다.**

- 세션 전역 말투 설정(축약형·caveman 등)은 **이 폴더에서 무효다.** 파일 내용·커밋 메시지·디스코드 문구 모두 `말투설정.md` 기준으로 쓴다
- `말투설정.md` 의 항목이 비어 있으면 같은 문서의 **"임시 기본값"** 절을 적용한다
- 사용자에게 보고할 때의 대화 말투는 세션 설정을 따른다 — **산출물 파일 내용만** 이 규칙의 대상이다

---

## 폴더 성격

| 항목 | 내용 |
| --- | --- |
| 정체 | 업무공유 리포트 정본. `tkcnco3436-ai/upmooshare` 클론 |
| 원격 | `origin` = `https://github.com/tkcnco3436-ai/upmooshare.git` (`main`) |
| 공개 URL | `tkcnco3436-ai.github.io/upmooshare/` — **저장소명 기준이므로 로컬 폴더명을 바꿔도 URL은 그대로** |
| 커밋 | 이 폴더는 **별도 저장소**다. 루트 저장소가 아니라 자기 원격으로 푸시한다 |

---

## 깃 경계

| 대상 | 깃 |
| --- | --- |
| `공유일지.md` · `말투설정.md` · `CLAUDE.md` | ✅ |
| `index.html` (공개 리포트 사이트) | ✅ |
| `YYYYMMDD업무공유.md` (깃용 날짜별 리포트 원본) | ✅ |
| `image/` · `0728_개발전달_공유/` · `보고서디자인.md` · `상인월드-소개서.md` | ✅ |
| **`YYYYMMDD_업무공유/` (날짜별 작업 폴더)** | 🚫 `.gitignore` 처리 |

**날짜별 폴더는 로컬 작업용이다.** 깃에는 공유일지와 공개 리포트만 올린다.

---

## 날짜별 폴더 규칙

```
YYYYMMDD_업무공유/
├─ 업무공유내용.md          작업 원본
└─ 디스코드용채팅공유.md     발송 문구
```

**이 2개 파일만 둔다.** 이미지·첨부가 필요하면 깃에 올라가는 `image/` 로 넣고 경로로 참조한다.

---

## 발행 절차 (요약)

1. `YYYYMMDD_업무공유/` 생성 → 파일 2개 작성
2. 확정 후 **깃용으로 별도 구성** — 루트 `YYYYMMDD업무공유.md` + `index.html` 리포트 블록 추가
3. 디스코드 발송
4. `공유일지.md` 에 한 줄 추가
5. `origin main` 푸시 → Pages 반영 확인
6. `../시스템에러/` 에 로그 (영역 `SYS`)

상세는 [`공유일지.md`](공유일지.md) 의 "발행 절차".

---

## 금지

- **과거 일자 리포트를 고치지 않는다.** `data-report-id="0720"` 등은 그날의 기록이다. 상태가 바뀌면 로드맵처럼 생존 문서에서만 갱신한다
- 공개 페이지이므로 **키·계정·내부 URL·개인정보**를 넣지 않는다
- 날짜별 폴더를 깃에 강제 추가(`git add -f`)하지 않는다
- 완료되지 않은 항목을 완료로 표기하지 않는다 — 지시가 있으면 표기하되 **미검증 사실을 사용자에게 알린다**

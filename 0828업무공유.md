# 0828 업무 공유 — 카드뉴스 생성 화면 개편

작성일: 2026-08-28 · 대상: 개발 전달 및 팀 공유

---

## 1. 요약

- **카드뉴스 생성 화면이 바뀌었습니다.** 단계가 줄고, 완료 화면이 새로 생기고, 편집 화면을 다시 만들었습니다.
- **템플릿 코드를 수정했습니다.** 템플릿이 들고 있는 값과 판정 방식을 정리했습니다.
- **생성 프롬프트를 수정했습니다.** 카드 분량 기준을 바꿨습니다.
- 바뀐 화면은 시안으로 전달합니다 (`0828_카드뉴스v2_공유`).

## 2. 화면 변경

### 생성 흐름

| | 이전 | 이후 |
| --- | --- | --- |
| 단계 | 5단계 | 3단계 — 템플릿 · 사진 · 내용 |
| 템플릿 고르기 | 고른 뒤 색까지 정하고 다음 | 고르면 바로 다음 |
| 사진 | 내용 적는 화면 안에 접혀 있음 | 별도 단계 |
| 완료 | 없음 (바로 편집) | **완료 화면 신설** — 내려받기 · 편집하기 |
| 편집 | 위자드 안 단계 | 위자드 밖 독립 화면 |

없앤 항목: 색 고르기 · 키워드 추천 · 분량 선택 · 사진 방식 선택 시트.

### 편집 화면

좌우 3분할에서, **카드가 화면 가운데를 크게 쓰는 구성**으로 바꿨습니다.

- 카드 목록은 아래 가로줄로 내려갑니다
- 글자를 **카드에서 직접 고칩니다** (오른쪽 입력칸으로 눈이 왕복하지 않습니다)
- 고른 글자 위에 작은 줄이 떠서 정렬·크기·색을 그 자리에서 바꿉니다
- 되돌리기 · 다시 실행을 넣었습니다
- 모바일은 도구가 하단 탭바 + 아래에서 올라오는 시트로 바뀝니다

카드를 그리는 **규칙**(레이아웃 · 슬롯 배치 · 색 구성)은 그대로고, **조작 방법**이 바뀌었습니다.
글자 규격과 장수 기준은 손봤습니다 — 같은 소재라도 이전과 같은 카드가 나오지는 않습니다.

## 3. 템플릿 코드 수정

템플릿이 사진 필요도를 **세 곳에 나눠** 들고 있었습니다. 값이 어긋나면 화면 표시와 실제 생성이 달라지는데 오류도 나지 않았습니다.

세 곳을 **한 곳으로 합쳤고**, 스펙에서 값을 계산해 화면이 보낸 값과 대조하도록 했습니다. 어긋나면 서버 기록에 경고가 남습니다.

카드 글자 규격도 스펙과 맞췄습니다 — 말풍선 · 글자 굵기 · 행간 · 자간.

## 4. 프롬프트 수정

카드 분량 기준을 바꿨습니다. 이전에는 소재가 짧으면 항상 최소 장수만 나왔습니다.

새 사실(없는 날짜·가격·연락처·품목)을 지어내는 것은 계속 금지입니다. 늘리는 방법은 **주어진 사실을 나눠 펼치는 것**으로 한정했습니다.

## 5. 카카오 로그인 — 심사 반려

카카오 개발자 센터 심사에서 반려됐습니다. 사유는 **사업자 정보 불일치**입니다 —
앱에 기입한 사업자는 「요즘」인데, 카카오 계정의 사업자는 「태경씨앤코」입니다.

- 두 정보를 맞추기 위해 **사업자 정보를 「태경씨앤코」로 임시 변경**해 다시 올립니다
- 통과된 뒤 원래 사업자로 되돌리는 순서입니다
- 되돌리는 시점에 재심사가 필요한지는 **확인이 필요합니다**

### 함께 바뀌어야 하는 표기

사업자 표기가 **「요즘」 → 「태경씨앤코 주식회사」**로 바뀝니다. 화면과 약관 문서에서 고쳐야 하는 곳입니다.

| 위치 | 바꿀 내용 |
| --- | --- |
| **푸터** | 상호 **태경씨앤코 주식회사** · 사업자번호 **823-88-03408** |
| **개인정보 처리방침** | `요즘(이하 '회사')` → `태경씨앤코 주식회사(이하 '회사')` |
| **이용약관** | 같은 치환 + **제18조(사업자 정보 및 연락처)** 의 상호 변경 |
| **개인정보 수집·이용 동의 (필수)** | `요즘(이하 '회사')` → `태경씨앤코 주식회사(이하 '회사')` |
| **마케팅 정보 수신 동의 (선택)** | `2. 광고성 정보 수신 동의` 항목의 같은 치환 |

- 약관·동의서는 **문구가 같은 자리에 여러 번** 나옵니다. 한 곳만 고치면 문서 안에서 상호가 엇갈립니다
- 푸터는 화면 전체에 깔리므로 **한 군데를 고치면 모든 화면에 반영**됩니다
- 사업자번호는 지금 푸터에 없던 값이라 **새로 넣어야 합니다**

## 6. 전달물

| 전달물 | 내용 |
| --- | --- |
| `0828_카드뉴스v2_공유` | 바뀐 화면 5종 독립 실행 스냅샷 (만들기 · 만드는 중 · 완료 · 편집기 · 내 카드뉴스). 폴더째 열면 동작합니다. 프롬프트 2종과 템플릿 스펙·정의 코드를 같이 넣었습니다 |
| `0828_web2_카드뉴스앱.zip` | **카드뉴스 생성 코드 전체** — 화면(생성 3단계 · 완료 · 편집기 · 내 카드뉴스) · API 라우트(사진 읽기 · 만들기 · 이미지 생성) · 템플릿 정의 · 스펙. `npm install` 후 `npm run dev` 로 돌아갑니다. **API 키는 들어 있지 않습니다** (`.env.example` 만 넣었습니다) |
| 템플릿 스펙 2종 | 글자 규격(행간·자간)이 기입된 스펙 파일 |
